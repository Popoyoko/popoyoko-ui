import './InputSelector.css';
interface InputSelectorProps {
    active: boolean;
    hover: boolean;
    label: string;
}
export declare const InputSelector: ({ active, hover, label, }: InputSelectorProps) => JSX.Element;
export {};

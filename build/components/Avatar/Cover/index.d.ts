import "./index.css";
import React from "react";
interface CoverProps {
    showedit?: boolean;
    background?: string;
}
export declare const Cover: ({ showedit, background }: CoverProps) => React.JSX.Element;
export {};

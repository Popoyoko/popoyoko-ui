import './index.css';
import React from 'react';
interface InputSelectorProps {
    hover: boolean;
    label: string;
}
export declare const InputSelector: ({ hover, label, }: InputSelectorProps) => React.JSX.Element;
export {};

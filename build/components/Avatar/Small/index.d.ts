import "./index.css";
import React from "react";
interface SmallProps {
    showbadge?: boolean;
    background?: string;
}
export declare const Small: ({ showbadge, background }: SmallProps) => React.JSX.Element;
export {};

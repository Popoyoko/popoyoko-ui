import type { Meta, StoryObj } from '@storybook/react';
import { Default } from './index';
declare const meta: Meta<typeof Default>;
export default meta;
type Story = StoryObj<typeof Default>;
export declare const default_card: Story;

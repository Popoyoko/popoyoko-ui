import "./index.css";
import React from "react";
interface SectionProps {
    title?: string;
    label?: string;
}
export declare const Section: ({ title, label, }: SectionProps) => React.JSX.Element;
export {};

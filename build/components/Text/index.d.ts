import "./index.css";
import React from "react";
interface TextProps {
    title: string;
    description: string;
}
export declare const Text: ({ title, description }: TextProps) => React.JSX.Element;
export {};
